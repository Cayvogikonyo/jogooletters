<h1> <%= @category.name %></h1>

<ul>
	<% @category.articles.each do |article| %>
		<li> <%= link_to article.title, article_path(article) %> </li>
	<% end %>
</ul>
<%= link_to "delete", category_path(@category), method: :delete, data: {confirm: "Do you want to delete the article?"} %><br>